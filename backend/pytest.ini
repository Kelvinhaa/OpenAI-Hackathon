[pytest]
# Put the backend/ root on sys.path so `backends` is importable under a bare
# `pytest` invocation. Without this only `python -m pytest` works, because that
# form implicitly prepends the current directory.
pythonpath = .
